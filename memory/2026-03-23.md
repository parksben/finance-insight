# Memory — 2026-03-23

## 飞书 Token/Key 更新（2026-03-23 04:03 CST）
- **旧 Verification Token**：`3dweyM3fpmhYxyVKZLW6OgkrCuOnboF7`
- **旧 Encrypt Key**：`yaGsH8IqrcS1M0gRcF5HAdRoi2qb5rcQ`
- **新 Verification Token**：`bUgDoidccdwqAhMnYGkcDcQzbbeiWyLY`
- **新 Encrypt Key**：`aPvpJnrmHKwxDW2MczI6agunBjWU2W7S`
- 已更新 `/root/.openclaw/openclaw.json`，已重启 Gateway
- 飞书 webhook 服务器正常启动（`127.0.0.1:3000`，path `/feishu/events`）
- **待排查**：飞书后台「事件订阅」Webhook URL 是否配置为 `https://zyx.parksben.xyz/webhook/feishu`，事件 `im.message.receive_v1` 是否已订阅

## CLIProxyAPI 管理界面（2026-03-23 04:x CST）
- `/management.html` 已通过 Caddy 路由在 `api.parksben.xyz/management.html` 暴露
- 访问需先通过 OTP 魔法链接认证，再输入 management key：`parksben-mgmt-key`
- Caddy 对 `/v0/management/*` 的 API 请求自动注入 `X-Management-Key` header
- Management UI 是内置 React 单页应用，含 upstream API key 映射、model mappings、OpenAI Compatible Providers 等功能模块

## 系统性能优化（完成）
- sessions.json 从 4.2MB 清理到 1.2MB（删除 90 条废弃 subagent/cron:run 记录）
- Skills 从 51 个归档到 37 个（归档目录：`/root/.openclaw/workspace/skills-archive/`）
- 已 git commit: `perf: archive 14 redundant skills (51→37), reduce skillsSnapshot overhead`
- niuma-tasks.md 已不在 workspace 根目录，每轮加载问题不存在

## antigravity-proxy 状态（故障）
- 路径：`/root/.antigravity-proxy/app/`，容器名：`antigravity-proxy`
- 容器 IP：`10.89.0.10:3000`（固定，重启后不变）
- 故障原因：账号 `aijuddinamanu@gmail.com` 触发 Google `validation_required`（OAuth challenge），不是 quota 耗尽
- 重置 accounts.json 后仍报 Quota Exhausted，根因是账号需要重新授权
- 账号 refreshToken 在 `/root/.antigravity-proxy/app/antigravity-accounts.json`

## 模型代理替换方案
- 现有方案：`frieserpaldi/antigravity-proxy v0.7.0`（已故障）
- 调研结论：
  - **CLIProxyAPI**（`router-for-me/CLIProxyAPI`）⭐19000+，**强烈推荐**
    - 专为 Gemini CLI OAuth 转 OpenAI 兼容 API
    - Go 语言，开源可审查，昨天更新 v6.9.0
    - 走官方 Gemini CLI，不是 hack，稳定
  - ZeroGravity（NikkeTryHard）⭐655，源码闭源，1个月历史，稳定性存疑
- **PengAn 买了 Google One 家庭版一年套餐**，想直接用这个额度
- 下一步：装 CLIProxyAPI，用 Google One 账号登录 Gemini CLI OAuth，替换 OpenClaw antigravity 配置
- 待确认：PengAn 是 Google One 家庭版 owner 还是成员

## OpenClaw 模型配置（当前）
- 配置文件：`/root/.openclaw/openclaw.json`
- 当前 primary model：`antigravity/claude-sonnet-4-6`
- fallback：`github-copilot/claude-sonnet-4.6`
- antigravity provider 端点：`http://10.89.0.10:3000/v1`，apiKey `no-key`

## CLIProxyAPI 部署进展（进行中）
- 二进制：`/usr/local/bin/cli-proxy-api` v6.9.0
- 配置文件：`/etc/cli-proxy-api/config.yaml`
  - host: 127.0.0.1, port: 18790
  - auth-dir: `~/.cli-proxy-api`
  - api-key: `parksben-cliproxy-key`
  - management secret-key: `parksben-mgmt-key`（路由 `/v0/management/*`，Header: `X-Management-Key`）
- systemd 服务：`cli-proxy-api.service`（enabled，开机自启）
- **旧服务清理完毕**：antigravity-proxy 容器已 stop/rm/rmi，配置目录已删，备份在 `/root/antigravity-accounts.json.bak`
- **Caddy 配置已修改**：`/etc/caddy/sites/aiclient2api.conf` reverse_proxy 改为 `127.0.0.1:18790`
- **认证尚未完成**：OAuth 回调地址是 `localhost:1455`，必须在本地机器跑，服务器上无法完成
- **等待 PengAn 操作**：
  1. Mac 上 `npm install -g @google/gemini-cli`，运行 `gemini` 完成 Google One 账号授权
  2. 把 `~/.gemini/oauth_creds.json` 内容发过来
  3. 我来放到服务器 + 更新 OpenClaw 配置 + 验证
- PengAn 是 Google One 家庭版 **owner**（已确认）

## OpenClaw 模型配置（需更新，等 CLIProxyAPI 认证完成后操作）
- 目标：将 `antigravity` provider 端点改为 `http://127.0.0.1:18790/v1`
- api-key 改为 `parksben-cliproxy-key`
- 模型 ID 需用 CLIProxyAPI 支持的 Gemini 模型（如 `gemini-2.5-pro`、`gemini-3-flash` 等）

## 用户偏好
- **回复语言：中文**（PengAn 明确要求，2026-03-23）
- 飞书 DM 是主要联系渠道（user: ou_ee5f996f246e05d7a5f158b96f5d9bcb）

## CLIProxyAPI OAuth 完成（2026-03-23 ~04:00 CST）
- PengAn 成功在 Mac 上完成 Google OAuth（gcloud application-default login）
- Google Gemini API Key 直接配置进 CLIProxyAPI（非 OAuth，用 API Key 模式）
- CLIProxyAPI 已正常启动，Gemini 模型调用验证通过
- OpenClaw 配置已更新（`/root/.openclaw/openclaw.json`）：
  - antigravity provider 端点：`http://127.0.0.1:18790/v1`
  - api-key：`parksben-cliproxy-key`
  - primary model：`gemini-2.5-flash`（通过 CLIProxyAPI 代理）
  - fallback：`github-copilot/claude-sonnet-4.6`

## CLIProxyAPI 管理仪表盘
- URL：`https://api.parksben.xyz/management.html`
- 需 OTP 魔法链接认证（Caddy forward_auth 保护）
- 认证通过后 Caddy 自动注入 `X-Management-Key: parksben-mgmt-key`，无需手动输入
- Caddy 配置已更新：`/etc/caddy/sites/aiclient2api.conf`
  - `/management*` 路由 → forward_auth + header_up X-Management-Key + reverse_proxy 18790
  - `/v0/management*` 路由 → 同上

## 飞书 Webhook 故障（2026-03-23 ~04:00 CST）
- 现象：PengAn 在飞书给我发消息收不到回复
- 日志中发现：`feishu[default]: webhook anomaly path=/feishu/events status=401 count=1`
- 根因：**飞书推过来的 webhook 签名验证失败**（401）
  - OpenClaw 里存的 verificationToken / encryptKey 与飞书后台不匹配，或飞书后台 Token/Key 已重置
- 当前 OpenClaw 配置中的值：
  - verificationToken：`3dweyM3fpmhYxyVKZLW6OgkrCuOnboF7`
  - encryptKey：`yaGsH8IqrcS1M0gRcF5HAdRoi2qb5rcQ`
  - appId：`cli_a925ba707ab8dbdf`，appSecret：`6bfDMpqvxou2yNHcZEKU1DW0bNZBd71y`
- **待处理**：PengAn 去飞书开放平台核对应用 Token/Key，如不匹配发给我同步更新
- 飞书 webhook URL 配置应为：`https://zyx.parksben.xyz/webhook/feishu`

## 架构现状（2026-03-23 04:00 CST）
- Caddy 负责 TLS 终止和路由（`/etc/caddy/sites/`）
- `zyx.parksben.xyz` → Caddy → 4090（OTP 认证网关） / 3000（OpenClaw Gateway webhook）
- `api.parksben.xyz` → Caddy → 8090（OTP 认证网关）+ 18790（CLIProxyAPI）
- OpenClaw Gateway：PID 10366，端口 18789（内部）+ 3000（webhook 入口）
- OTP 认证网关（auth-gateway）：PID 10391（node，`/opt/openclaw-auth-gateway/server.js`），端口 4090
- CLIProxyAPI：systemd `cli-proxy-api.service`，端口 18790（API）/18791（mgmt）
