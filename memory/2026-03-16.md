# 2026-03-16 Memory

## WeCom Plugin Fix (13:30)
- **Root cause:** Plugin id mismatch — `openclaw.plugin.json` declared `"wecom-openclaw-plugin"` but JS export used `"wecom"`
- **Fix:** Changed `openclaw.plugin.json` id to `"wecom"`, updated `openclaw.json` plugins.allow/entries/installs keys to match
- **Result:** WeCom channel came online (ON/OK), PengAn confirmed receiving messages at 13:32

## Niuma v0.1.0 Release (13:55–14:11)
- Set `CROSS_REPO_TOKEN` secret on niuma-desktop, niuma-app, niuma-cli (were missing; niuma-server & niuma-web already had it)
- Bumped versions: niuma-web 0.0.0→0.1.0, niuma-app 0.0.1→0.1.0 (others already 0.1.0)
- Fixed niuma-web TS build error: removed unused `accent`/`trackFill` vars in `AudioBubble.tsx` (commit `91813e8`)
- Tagged v0.1.0 on all 4 repos: niuma-desktop, niuma-app, niuma-server, niuma-cli
- All CI runs triggered and completed successfully
- Release assets on `parksben/niuma` v0.1.0: desktop (macOS dmg, Linux deb/rpm/AppImage, Windows exe/msi), Android APK, server binaries (linux/mac/win), CLI binaries (linux/mac/win)

## Cron Tasks Removed (~17:00)
- PengAn requested cancellation of niuma-patrol (30min) and niuma-report (2hr) cron jobs
- Both removed successfully

## CLI Install Script Fix (00:29)
- **Issue:** `install.sh` hung at download step — ghproxy probe returned 200 but actual download timed out with no `--max-time`
- **Fix:** Changed to try GitHub direct first (with 120s timeout), fallback to ghproxy only on failure (commit `20268ed` on niuma-cli)
- PengAn was testing on macOS; direct GitHub download should work fine for him

## Config Warning (ongoing)
- `plugins.entries.wecom` still shows mismatch warning: "manifest uses 'wecom', entry hints 'wecom-openclaw-plugin'"
- Functional but needs cleanup in `openclaw.json` installs section (the `installs` key still references old name)

## Gateway Stability
- Multiple `gateway closed (1000)` errors during cron operations around 16:56–18:27
- Gateway restart via CLI failed with SIGTERM; eventually recovered on its own

## Install Script Improvements (00:33–01:20)

### awk → bc fix
- macOS BSD awk choked on float division for file size formatting
- Replaced with `bc -l` — cross-platform compatible

### curl | bash stdin fix
- `read -p` consumed script body as input when piped via `curl | bash`
- Fix: `exec < /dev/tty` before interactive reads

### Parallel chunked download
- GitHub CDN supports `Range` requests (verified 206 + content-range)
- Each file split into 4 chunks downloaded concurrently, 2 files in sequence
- Real-time progress bar per file

### Remove interactive config wizard
- Removed Gateway URL/Token/Port prompts from install script entirely
- Generate default `~/.niuma/config.json` silently
- Config happens in Web UI SetupPage after first launch (commit `9f36d1e`)

### Auto-download desktop client
- After server install, auto-downloads platform-appropriate desktop app (.dmg/.AppImage/.exe) to ~/Downloads
- Opens Downloads folder automatically (open/xdg-open/explorer)
- Falls back to printing Releases URL if download fails (commit `a876f35`)

## Settings Page & Setup Flow (01:00–01:15)

### niuma-server
- New `GET/PUT /api/settings` endpoint — reads/writes `~/.niuma/config.json`
- Token returned as `••••••••` (masked), with `OPENCLAW_GATEWAY_TOKEN_SET` boolean
- No auth middleware on this endpoint (intentional — needed before login)
- Commit `642c341`

### niuma-web
- New `SetupPage.tsx` — first-run config wizard (Gateway URL + Token + Port)
- Amber guide card: "在服务器上运行 `openclaw token`" with docs link
- Router flow: `/setup` → checks if token configured → redirect to `/login` if yes
- `LoginPage` also checks settings on mount, redirects to `/setup` if token not set
- `ProfilePage` rewritten with full settings form (was empty placeholder)
- Required field validation: Token mandatory on first config, Gateway URL always required
- Commits: `a57d35c`, `7d0bc0b`, `fec45d0`

## CI Fix: Cross-Repo Web Build (01:10–01:20)
- **Problem:** `dawidd6/action-download-artifact@v3` incompatible with `actions/upload-artifact@v4` — artifact not found
- **Fix:** Both niuma-server and niuma-desktop CI now `git clone` niuma-web with `CROSS_REPO_TOKEN` and build from source
- niuma-web is private repo, needed `x-access-token:${{ secrets.CROSS_REPO_TOKEN }}` in clone URL
- Commits: `add03e1` (server), `5f63c53` (desktop)
- v0.1.1 tags re-pushed on both repos, CI triggered — monitoring in background

## README Cleanup (01:15)
- `parksben/niuma` README rewritten: removed all private repo links, tech stack table
- Pure user-facing: product intro → quick start → download → architecture
- Force-pushed commit `996ef98`

## Pending CI (01:20)
- niuma-server and niuma-desktop v0.1.1 builds in progress (session `quiet-reef` monitoring)
- Previous 2 rounds failed: (1) dawidd6 v3/v4 incompatible, (2) private repo clone without auth
- Current round should work with authenticated git clone

## Cron Tasks Active
- `niuma-patrol` (d92003da): every 30min, silent inspection + auto-fix
- `niuma-report` (cd73054f): cron `0 */2 * * *` Asia/Shanghai, sends progress to PengAn via WeCom

## v0.1.1 Build Success (01:28)
- All CI passed: niuma-server ✅, niuma-desktop ✅, niuma-cli ✅
- Release assets on `parksben/niuma` v0.1.1: server (5 platforms), desktop (macOS/Linux/Windows), CLI (5 platforms)
- niuma-cli needed separate v0.1.1 tag push (was missing, caused install.sh 0B file size)

## install.sh Fixes (01:30–01:40)
- **0B file size bug:** macOS curl `-I -L` doesn't follow redirects properly for content-length; fixed by using `-r 0-0` Range request + parsing `content-range` header (commit `9614aab`)
- **8 chunks:** Changed `CHUNKS=4` to `CHUNKS=8`, removed "4 分片并发下载" hint text (commit `f6121f8`)

## CLI spawn node ENOENT Fix (01:41–01:50)
- **Root cause:** CLI `server start` used `spawn('node', ['index.js'])` but server is now a compiled Bun binary
- **3 locations fixed:**
  1. `systemd.js` nodeStart() → `spawn(serverBin, [])` where `serverBin = ~/.niuma/bin/niuma-server`
  2. `systemd-install.js` → `ExecStart=${serverBin}` instead of `${nodebin} ${entry}`
  3. `install.js` fallback spawn → same binary path
- Commits: `f3ea67e`, `b4e0808`
- **Remaining tech debt:** `niuma install` still does `git clone niuma-server` + `npm install` (should use prebuilt binary); `niuma config` still asks SMTP (should be in Web UI)

## v0.1.2 Triggered (01:48)
- **Login page rewrite:** username/password → email OTP (6-digit code input, auto-submit, paste support, 60s countdown)
  - Auth API: `sendCode(email)` → `verifyCode(email, code)` matching server's `/api/auth/send-code` + `/api/auth/verify-code`
  - Theme color changed from purple `#6366F1` to orange `#FF6B35`
  - Web commit `0664dcd`
- **Logo on login page:** Replaced 🐂 emoji with origami logo image (`https://assets.parksben.xyz/images/78c9c0c0501b23be.png`)
- **Dock icon padding:** Regenerated all icon sizes with 80% content / 20% padding for macOS dock consistency (commit `e6bc818` on niuma-desktop)
- Tags v0.1.2 pushed on niuma-server + niuma-desktop, builds triggered

## LoginPage Auto-detect Server (01:50)
- Rewrote LoginPage to 3-step flow: **server → email → code**
- On load, probes `localhost` ports `[3002, 3000, 8080, 8000, 3001]` via `/health`
- Found → auto-fill + skip to email step, green banner "已连接到 ..."
- Not found → yellow warning card with `niuma server start` hint + manual URL input
- "🔄 重新检测本地服务" button, "⚙️ 更换服务器地址" link from email step
- Commit `bd605fc` on niuma-web

## Full-Stack Code Review (01:56)
- Spawned subagent for comprehensive review across all 5 repos
- **Critical findings (5):**
  - C1: `/api/settings` no auth — anyone can read/write Gateway Token
  - C2: `/api/employees` called by frontend but not implemented in server (404)
  - C3: JWT_SECRET hardcoded fallback `'dev_secret'`
  - C4: CLI install still `git clone` + `npm install` server source (should use prebuilt binary)
  - C5: Port conflict → `kill -9` arbitrary process
- **Important (9):** `/api/connect/qr-data` & `/api/templates` no auth, route dual-mount, DB no indexes, field naming inconsistency (snake vs camel), axios 10s timeout truncates SSE, port default mismatch (3000 vs 3002), version mismatch (web still 0.1.0), etc.
- **Nice-to-have (8):** README missing key steps, DB migration errors silenced, CLI/server config.json conflict, Tauri CSP null, no global error handler, etc.
- Plan: fix Critical first (C1→C3→C5→C2→C4), then release v0.1.3

## Patrol Task Updated (02:10)
- PengAn requested: add full architecture/flow review to 30-min patrol cron
- Updated `niuma-patrol` cron message to include 6 dimensions:
  1. CI/CD status  2. Server architecture (API/auth/errors/DB)  3. Server↔Web alignment  4. CLI flow  5. README readability  6. Cross-project consistency
- Rule: fix+push directly, notify PengAn only when changes made, silent otherwise
- Patrol prompt saved to `/root/.openclaw/workspace/niuma-patrol-prompt.md`
- Gateway was unstable during update (repeated 1000 close), needed multiple retries

## Gateway Instability (02:00–02:24)
- Gateway kept disconnecting (1000 normal closure) during cron operations
- `openclaw gateway stop/start` caused SIGTERM
- Eventually recovered after restart

## Niuma v0.1.2 Build Fix (02:52)
- **CLI 9B bug**: niuma-cli 没打 v0.1.2 tag，导致 release assets 没上传，install.sh 下载到 9B 空文件
- **Desktop version mismatch**: tauri.conf.json 仍为 0.1.1，产物文件名显示旧版本
- **Fix**: 三个仓库(cli/desktop/server)都更新 version 到 0.1.2 并重新打 tag 触发构建
- **巡检加强**: niuma-patrol 新增第 7 条"构建产物检查"（CLI>5MB, Server>50MB, Desktop>3MB, 版本号一致性）

## SMTP 配置链路打通 (03:11)
- **问题**: SetupPage 只配了 Gateway，没配 SMTP，邮件发不出去
- **Server 改动**:
  - `settings.js` 支持 SMTP 字段读写（SMTP_HOST/PORT/USER/PASS/FROM/SECURE）
  - 保存后热更新 nodemailer transporter（不用重启）
  - `smtp.js` 增加未配置时友好报错 + resetTransporter 导出
  - `index.js` 启动时从 `~/.niuma/config.json` 加载 SMTP 到 env
- **Web 改动**: SetupPage 改为 2 步流程（Step1 Gateway → Step2 SMTP），内置 QQ/163/Gmail 引导
- **Commits**: server `b078e6c`, web `4b2935f`

## 巡检加第 8 条：产品一致性检查 (03:14)
- 各模块实现是否与产品设计一致、前后端 API 对齐、配置项同步、功能链路端到端、版本/Logo/文案统一

## 用户管理系统（白名单登录）(03:14)
- **Admin 机制**: 首次配置 SMTP 时，SMTP 邮箱自动成为 admin
- **Server**: `users` 表加 `role` 字段(admin/user)，`/api/admin/users` CRUD（需 admin 权限）
- **白名单**: `send-code` 检查邮箱是否已注册，未授权返回 403
- **Web**: AdminPage（/admin 路由），添加/删除用户、升降权限
- **Commits**: server `1799a66`, web `deabc91`

## AI 辅助生成员工 (03:14)
- **Server**: `POST /api/ai/generate-employee`，接收 prompt 或 templateKey，调 OpenClaw 生成完整员工信息(name/roleType/bio/duties/style/skills)
- **Web**: EmployeesPage 创建对话框重构为 3 种模式：
  1. ✨ AI 一键生成（自由描述）
  2. 📋 模板选择 + AI 补全
  3. 预览编辑（所有字段可调）
- **Commits**: server `a670fcf`, web `ef68ca9`

## 多用户聊天支持 (03:20)
- **chat_members 表**: (chat_id, user_id, role, joined_at)
- **chatAccess() helper**: owner 或 member 都能访问聊天
- **GET /chats**: 返回自己创建的 + 被邀请参与的
- **成员管理 API**: GET/POST/DELETE /chats/:id/members（邀请/踢人/退出）
- **创建群聊时**: creator 自动加入 chat_members，支持 user_ids 参数邀请其他用户
- **Commit**: server `0de4052`

## Skill Scout 定时任务 (02:43)
- **Job**: `skill-scout` (id: `5a5362e0-9f0c-4fce-b034-b116996f4cce`)
- **Schedule**: `0 3,9,15,21 * * *` Asia/Shanghai（每6小时）
- **功能**: 自动从 ClawhHub 搜索实用 skill，审核安全性，安全则自动安装+测试
- **搜索领域**: 节约 token、软件研发、工程研发、产品设计、UI 设计

## niuma-report 改为每天3次 (02:30)
- 从 `0 */2 * * *` 改为 `0 9,12,18 * * *`（早9中12晚6）

## Security Fixes C1+C3+C5 (03:34)
- **C1**: `/api/settings` now requires admin auth via `settingsAuth` middleware; first-time setup (no admin exists) exempt
- **C3**: JWT_SECRET auto-generated with `crypto.randomBytes(48)`, persisted to `~/.niuma/.jwt_secret` (mode 0o600), no more `'dev_secret'` fallback
- **C5**: Port conflict `EADDRINUSE` now exits with message instead of `kill -9` arbitrary processes
- **Commit**: server `0ec1af8`

## Web UX Improvements (03:34)
- LoginPage: detects `smtp` field from `/health` response, shows yellow warning if SMTP unconfigured ("验证码将显示在终端")
- Desktop + Mobile nav: added ⚙️ 管理 (admin) link
- `/health` endpoint now returns `smtp: boolean`
- AdminPage TS fix: `type User` → `type` keyword import for verbatimModuleSyntax
- **Commits**: web `a216676`

## niuma-app SetupScreen Update (03:40)
- Replaced 🐂 emoji with origami bull logo (`LOGO_URL`)
- Connect button changed from `#FFD700` (gold) to `#FF6B35` (orange), text white
- Added `Image` import from react-native
- App is WebView-based, so web's OTP login automatically applies to mobile
- **Commit**: app `dd1ce85`

## v0.1.2 Build Status (03:47)
- Desktop v0.1.2 rebuild triggered (tag recreated), in progress
- Server v0.1.2 build SUCCESS — assets uploaded to `parksben/niuma` release
- CLI v0.1.2 assets confirmed normal size (59-111MB)
- Desktop old assets still show 0.1.1 filenames, waiting for new build
- **Run IDs**: desktop `23117761458`, server `23117762138`

## Multi-user Chat (03:20, detailed)
- `chatAccess()` helper: LEFT JOIN chat_members, allows owner OR member
- GET /chats: `SELECT DISTINCT c.* ... WHERE c.user_id = ? OR cm.user_id = ?`
- POST /chats/group: accepts `user_ids` array, auto-adds creator as `role='owner'`
- Member mgmt: GET/POST/DELETE `/chats/:id/members`; invite by `user_ids` or `emails`; only owner can invite/remove; users can leave themselves
- **Commit**: server `0de4052`
