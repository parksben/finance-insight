# Memory — 2026-03-12

## 牛马（niuma）项目进展

### 仓库地址
- niuma (Flutter App): https://github.com/parksben/niuma — 本地: `/root/.openclaw/projects/niuma/`
- niuma-server: https://github.com/parksben/niuma-server — 本地: `/root/.openclaw/projects/niuma-server/`
- niuma-cli: https://github.com/parksben/niuma-cli — 本地: `/root/.openclaw/projects/niuma-cli/`

### niuma-server
- 已部署：`/opt/niuma-server/`，入口 `index.js`，端口 3002
- systemd: `niuma-server.service`，已 enable
- health: `curl http://localhost:3002/health` → `{"status":"ok"}`
- 技术栈：Node.js + Express + SQLite（sqlite3，非 better-sqlite3）
- 认证：邮箱 OTP + JWT（7天有效期）
- .env 位置：`/opt/niuma-server/.env`
- 子 Agent `niuma-server-kb` 正在为 server 添加知识库功能（session: agent:main:subagent:7d1cbc46-4e23-46a2-88bd-26448d2e9fd8）

### niuma-cli
- 最新版本已推送 main，删除了 master 分支
- `niuma install` 完整 5 步流程已实现并验证：
  1. 检测 OpenClaw（支持手动指定路径 `--openclaw-path`，支持多实例）
  2. SMTP 配置
  3. 创建 5 个 OpenClaw Agent（openclaw agents add CLI，非 HTTP API）
  4. 部署 niuma-server（git clone + npm install + systemd）
  4.5. HTTPS 配置（可选：Caddy 反代 + DNS 检测）
  5. 打印完整配置摘要（服务地址、JWT secret、Agent 列表）
     内网 IP 自动检测，提示用户确认同一局域网
- Gateway 端口自动从 `openclaw gateway status` 动态读取（当前 18789）
- systemd 入口文件已修正为 `index.js`

### niuma App
- Flutter 代码在 `apps/mobile/`
- 子 Agent `niuma-app-kb` 正在添加知识库 UI（session: agent:main:subagent:223742bb-194b-454c-8adc-6e79912f1fdf）
- 待实现：ServerConnectScreen（首次启动引导填写 server 地址 + /health 验证）
- CI：Debug APK 构建已验证 success（最新 run: 22949152357）

### 5 个 OpenClaw Agent（已在本机创建）
- niuma-planner（规划师 📋）
- niuma-coder（工程师 💻）
- niuma-designer（设计师 🎨）
- niuma-analyst（分析师 📊）
- niuma-writer（文案 ✍️）

### 知识库功能设计（进行中）
- DB 表：`knowledge_docs`（id, project_id, title, content, author_type, author_id, author_name, created_at, updated_at）
- REST API：GET/POST/PUT/DELETE `/api/projects/:id/docs`
- Agent 上下文注入：发消息时把知识库摘要附在 system context
- Agent 写文档：回复中嵌入 `<!-- create_doc: {"title":"..."} -->内容<!-- /create_doc -->`，服务端解析入库
- App：项目群 → 知识库 Tab，支持 Markdown 渲染 + 编辑

## 进度同步 Cron
- ID: `c25a371a-7143-498b-92d8-5a0561dde9e8`，每两小时（偶数小时整点）推送牛马进展给 PengAn（wecom）

## finance-insight 数据修正
- 518880 黄金ETF 建仓价修正为 10.974（3月11日实际收盘价）
- 份数：9113，止损：10.43（-5%）
- portfolio.json 已对齐 Vue 组件字段名
- update-portfolio 脚本：`/usr/local/bin/update-portfolio`，支持 buy/sell/price/sync
  - transactions 字段：asset, action(买入/卖出), shares, price, reason
  - positions 字段：weight(小数 0.1), cost, price

## OpenClaw 关键信息
- gateway PID: 运行中，端口 18789（loopback）
- openclaw-backup cron: 每天 00:00 CST（ID: 30b02482-83b7-42ba-9f76-507a172b876c）
- niuma-progress-sync cron: 每2小时（ID: c25a371a-7143-498b-92d8-5a0561dde9e8）

## 待办
- [ ] niuma-server-kb 子 Agent 完成后验证知识库 API
- [ ] niuma-app-kb 子 Agent 完成后检查 Flutter 编译
- [ ] App 添加 ServerConnectScreen
- [ ] App 对接真实 niuma-server API
- [ ] Caddy HTTPS 配置（api.parksben.xyz → localhost:3002）
- [ ] niuma App CI：确认 release APK 签名构建
- [ ] niuma-startup 仓库手动删除（用户操作：https://github.com/parksben/niuma-startup/settings）
